# Tenant-defined roles over platform permissions

CloudForge uses tenant-scoped RBAC in which platform and domain services define stable Permission codes, while Tenant administrators may create Tenant Roles that group those Permissions and assign them through Tenant Memberships. Cross-tenant Platform Roles remain a separate platform-controlled authority and cannot be derived from Tenant Roles.
