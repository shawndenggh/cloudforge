# Kubernetes-only production platform

CloudForge targets Kubernetes as its only production runtime so that service discovery, load balancing, health management, configuration injection, and workload orchestration can rely on the platform rather than parallel VM-oriented infrastructure. Local development may use lighter workflows, but production behavior and deployment artifacts must remain Kubernetes-native.
