# Java and Spring platform baseline

CloudForge starts on Java 25 LTS, Spring Boot 4.1.0, Spring Cloud 2025.1.2 (Oakwood), and Gradle 9.6.1. The Gradle Wrapper, Java toolchain, version catalog, CI environment, and container runtime pin this compatible baseline so that upgrades are explicit repository-wide changes rather than independent module drift.
